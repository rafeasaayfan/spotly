<script setup lang="ts">
import Edit from '@/components/table/actions/Edit.vue';

const props = defineProps<{
{{ props }}
}>();

const mappedOrders = props.orders.map((order) => ({
    value: order.id,
    label: order.delivery_address,
}));

const columns = [
{{ columns }}
];
</script>

<template>
    <Edit :data="props.data" :columns="columns" table="{{ slug }}" href="{{ href }}" />
</template>
