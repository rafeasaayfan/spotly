<script setup lang="ts">
import Create from '@/components/table/actions/Create.vue';

const props = defineProps<{
{{ props }}
}>();

//const mappedProps = props.orders.map((item) => ({
//    value: item.id,
 //   label: item.name,
//}));

const columns = [
{{ columns }}
];
</script>

<template>
    <Create :columns="columns" table="{{ slug }}" />
</template>
